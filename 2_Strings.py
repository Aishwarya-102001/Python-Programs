name="ada lovelace"
print(name,"\n")  
print(name.title(), "\n") #title() changes each word to title case
print(name.upper(), "\n") #upper() changes each word to upper case
print(name.lower(), "\n") #lower() changes each word to lower case
Strip_whitespaces=' Python is a language ' 
print(Strip_whitespaces.rstrip())  #strips whitespaces from right side
print(Strip_whitespaces.lstrip())  #strips whitespaces from left side
print(Strip_whitespaces.strip())   #strips whitespaces from both side
